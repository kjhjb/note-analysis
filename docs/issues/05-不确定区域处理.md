# 05 — 不确定区域处理

**What to build:** AI Agent（复用 Ticket 01 的 Agent 框架 + Anthropic 兼容协议）汇总 Ticket 04 标记的所有低置信度区域（`confidence < threshold`），调用 LLM 对每个不确定区域生成文本猜测。在 Web UI 中统一展示，用户可接受猜测、手动输入、或忽略。确认后更新 JSON。此票的 LLM 调用与 Ticket 04 共用同一 Agent 编排框架。

**Blocked by:** 04 — 识别引擎

**Status:** ready-for-agent

- [ ] Agent 读取 `uncertainRegions[]`（含低置信度裁剪图和 LLM 原始猜测）
- [ ] Agent 调用 LLM 对每个不确定区域生成更精确的文本猜测（复用 Ticket 01 的 Agent 框架）
- [ ] Web UI 展示：裁剪缩略图 + Agent 猜测文本 + 选择/输入框
- [ ] 用户交互："接受猜测"／"输入正确文本"／"忽略此区域"
- [ ] 确认后 Agent 更新 `userConfirmed` 字段到 JSON
- [ ] 全确认后方可进入下一阶段

**Blocking:** 06 合理性审查
